# Flyway configuration for V3__Add_suspended_to_task_status.sql
# This migration must run outside a transaction because:
# 1. ALTER TYPE ADD VALUE cannot run inside a transaction block in PostgreSQL ≤ 11
# 2. Avoids potential ALTER TYPE transaction pitfalls in general
executeInTransaction=false